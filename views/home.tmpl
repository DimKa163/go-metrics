﻿<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <div class="metrics">
        {{range .metrics}}
        <div class="metric">
            <h2>{{ .Name }}</h2>
            <p>{{ .Value }}</p>
        </div>
        {{end}}
    </div>
</body>
</html>